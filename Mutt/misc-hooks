folder-hook '.'          'set sort_aux=date-received'
folder-hook 'Inbox'      'set sort_aux=score'
folder-hook 'List*'      'push <limit>~N|~O<enter>'
folder-hook 'Archive'    'push <undelete-pattern>~D<enter>'
folder-hook 'Monitoring' 'push <limit>~N|~O<enter>'
folder-hook 'Jira'       'push <limit>~N|~O<enter>'
folder-hook 'Commons'    'push <limit>~N|~O<enter>'
folder-hook 'SAMS'       'push <limit>~N|~O<enter>'
folder-hook 'CNOC'       'push <limit>~N|~O<enter>'

# vim: ft=muttrc
